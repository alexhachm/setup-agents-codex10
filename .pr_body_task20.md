## Summary
- add coordinator web status contract tests covering routing budget telemetry fields and enriched task telemetry hydration
- verify both populated telemetry and null fallback behavior in `/api/status`
- add dashboard rendering regressions for routing/model/source/effort chips and budget indicator presence/absence handling

## Validation
- cd coordinator && node --test tests/web-server.test.js tests/dashboard-render.test.js
- cd coordinator && npm test
