### 2026-03-29 — gap-detection utility (task 22)
New domain "newdomain" established for utility functions. Pattern: single-purpose JS module in coordinator/src/ + matching test file in coordinator/tests/. Tests use node:test + node:assert. The detectGaps(sequence) function finds missing integers between min and max of a numeric array.
